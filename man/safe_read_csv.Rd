% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repoquet.R
\name{safe_read_csv}
\alias{safe_read_csv}
\title{Safely read a CSV file with UTF-8 sanitisation.}
\usage{
safe_read_csv(path)
}
\arguments{
\item{path}{Character. Full path to the \code{.csv} file.}
}
\value{
A \code{data.table}, or an empty \code{data.frame()} if reading
  fails.
}
\description{
Wraps \code{data.table::fread()} with \code{tryCatch}.  All character
columns are re-encoded to UTF-8 after loading.
}
\examples{
\dontrun{
set.seed(1)
tmp_csv <- tempfile(fileext = ".csv")
df <- data.frame(AGE = sample(18:90, 20, replace = TRUE),
                 SEX = sample(c("M", "F"), 20, replace = TRUE) )
write.csv(df, tmp_csv, row.names = FALSE)
df_loaded <- safe_read_csv(tmp_csv)
str(df_loaded)
unlink(tmp_csv)
}
}
\seealso{
\code{\link{safe_read_sav}}
}
