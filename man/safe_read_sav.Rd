% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repoquet.R
\name{safe_read_sav}
\alias{safe_read_sav}
\title{Safely read a SAV file with UTF-8 sanitisation}
\usage{
safe_read_sav(path)
}
\arguments{
\item{path}{Character. Full path to the \code{.sav} file.}
}
\value{
A \code{data.table} with all columns in their declared base types, or
  an empty \code{data.frame()} if reading fails.
}
\description{
Wraps \code{haven::read_sav()} with \code{tryCatch}.  After loading,
\pkg{haven} S3 attributes are stripped via \code{strip_haven} and all
character columns are re-encoded to UTF-8 (replacing unmappable bytes with
their hex escape).
}
\examples{
\dontrun{
set.seed(1)
tmp_sav <- tempfile(fileext = ".sav")
df <- data.frame(AGE = sample(18:90, 20, replace = TRUE),
                 SEX = haven::labelled(sample(1:2, 20, replace = TRUE), c(Male = 1, Female = 2)) )
haven::write_sav(df, tmp_sav)
df_loaded <- safe_read_sav(tmp_sav)
str(df_loaded) # SEX is now a plain integer, not haven_labelled
unlink(tmp_sav)
}
}
\seealso{
\code{\link{safe_read_sav_chunked}}, \code{strip_haven}
}
