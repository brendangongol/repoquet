# Healthcare workflow references

These scripts preserve the CECORC healthcare workflow that drove development of
\`repoquet\`. They are included as implementation references and are not run by
the package, tests, examples, or command-line interface.

The scripts contain machine-specific paths and require licensed source data.
Use \`create_repository_project(profile = "generic")\` for a new repository
instead of executing these files unchanged.
